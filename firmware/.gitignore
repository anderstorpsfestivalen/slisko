# Rust build output
/target
**/target

# esp-idf-sys / embuild: the downloaded ESP-IDF + toolchain (multi-GB). NEVER commit.
.embuild/
**/.embuild/

# esp-idf generated config (sdkconfig.defaults IS source and stays tracked)
**/sdkconfig
**/sdkconfig.old

# Managed-component lock (regenerated from idf_component.yml)
**/dependencies.lock

# NOTE: slisko-fw/src/generated_config.rs is generated by `cmd/baker` but is
# kept tracked — the firmware won't build without it. Regenerate with the baker
# when a config changes.
