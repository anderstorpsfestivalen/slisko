[package]
name = "host"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
engine = { path = "../engine", features = ["std"] }
config = { path = "../config" }
ddp-rs = "1.3.0"
clap = { version = "4.6.6", features = ["derive"] }
chrono = "0.4.45"
